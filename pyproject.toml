[build-system]
requires = ["setuptools>=77,<83"]
build-backend = "setuptools.build_meta"

[project]
name = "go2-pro-energy-planner-offline"
version = "0.2.0"
description = "Offline analysis companion for go2-pro-energy-planner"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{name = "Gabriel Demetrios Lafis"}]
dependencies = ["jsonschema>=4.23,<5"]

[project.scripts]
go2-energy = "go2_energy.__main__:main"

[tool.setuptools.packages.find]
where = ["python"]
exclude = ["tests*"]

[tool.setuptools.package-data]
go2_energy = ["scenario.schema.json", "sql/*.sql"]

[tool.ruff]
line-length = 100
target-version = "py311"
